Custom Options:

You may set the maximum limit to show by adjusting the slider options.  Enabling a large amount of buttons on this bar could cause lots of interface lag.  The UI must be reloaded after these values are adjusted.

Note that if new mounts are added, you must reload the UI to show them.